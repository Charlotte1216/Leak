# Comparison Algorithms

This directory is reserved for comparison or baseline algorithm
implementations.

Suggested usage:
- Put one algorithm in one module or subpackage.
- Keep shared helper code local to this directory.
- Keep experiment configs under `marl_leakage_search/configs/`.

Example candidates:
- `ippo.py`
- `vdn.py`
- `heuristic_search.py`
