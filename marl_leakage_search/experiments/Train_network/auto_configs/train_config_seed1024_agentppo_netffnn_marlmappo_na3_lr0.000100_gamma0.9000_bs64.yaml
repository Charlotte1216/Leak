training:
  num_episodes: 8000
  max_steps_per_episode: 1000
  save_interval: 100
  eval_interval: 50
  log_interval: 10
  pretrained_dir: ''
marl:
  algorithm: mappo
  mappo:
    gamma: 0.9
    gae_lambda: 0.95
  qmix:
    gamma: 0.99
    mixer_lr: 3e-4
    tau: 0.005
    global_state_dim: 8
environment:
  num_agents: 3
  state_dim: 4
  action_dim: 8
  source_find_radius: 5.0
  collision_penalty: 1.0
  battery_penalty: 0.001
  found_source_bonus: 20.0
  done_bonus: 0.0
  success_done_bonus: 120.0
  failure_done_penalty: 5.0
  enable_collision: true
  stop_on_collision: false
  observe_wind: true
  distance_reward_scale: 6
  found_source_concentration_scale: 0.05
  found_source_stay_penalty: 0.15
  found_source_stay_radius_scale: 1.5
  uav_params:
    max_speed: 5.0
    max_acceleration: 1.0
    max_battery: 150.0
    energy_consumption_rate: 0.07
    dt: 1.0
  init_pos_mode: random
output:
  save_dir: ./checkpoints
  log_dir: ./logs
  tensorboard_dir: ./runs
seed: 1024
